% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Matrix.R
\name{matrix_variate_mixture_fit}
\alias{matrix_variate_mixture_fit}
\title{Fit Matrix-Variate Gaussian Mixture Model via EM Algorithm}
\usage{
matrix_variate_mixture_fit(
  x_list,
  g,
  max_iter = 100,
  tol = 1e-06,
  nstart = 10,
  verbose = FALSE
)
}
\arguments{
\item{x_list}{A list of numeric matrices, each of dimension r × p}

\item{g}{Integer: number of mixture components}

\item{max_iter}{Integer: maximum EM iterations (default: 100)}

\item{tol}{Numeric: convergence tolerance for log-likelihood (default: 1e-6)}

\item{nstart}{Integer: number of k-means restarts for initialization (default: 10)}

\item{verbose}{Logical: print iteration progress (default: FALSE)}
}
\value{
A list containing:
\item{pi}{Numeric vector of length g: final mixing proportions}
\item{M}{List of g matrices: final component mean matrices}
\item{U}{List of g matrices: final row covariance matrices}
\item{V}{List of g matrices: final column covariance matrices}
\item{z}{Numeric matrix (n × g): posterior responsibilities
\item{cluster}{Integer vector of length n: hard cluster assignments}
\item{logLik}{Numeric vector: log-likelihood trace across iterations}
\item{iterations}{Integer: number of EM iterations performed}
\item{converged}{Logical: whether algorithm converged within max_iter}
}
\description{
Estimates parameters of a matrix-variate Gaussian mixture model (MGMM)
using the Expectation-Maximization algorithm. Performs clustering of
matrix-valued observations while accounting for row and column dependencies.
}
\details{
The EM algorithm alternates between:
\strong{E-step:} Compute posterior responsibilities (soft cluster assignments)
\strong{M-step:} Update parameters based on responsibilities
}
\examples{
\dontrun{
set.seed(123)
mean_1 <- matrix(c(1.5, 1.2, 1.0, 1.3, 1.1, 1.4, 1.2, 1.0), nrow=2)
mean_2 <- matrix(c(-1.4, -1.0, -1.2, -1.3, -1.1, -1.5, -1.0, -1.2), nrow=2)

simulate_matrix_group <- function(n, mean_matrix, row_sd=0.35, col_sd=0.35) {
  r <- nrow(mean_matrix); p <- ncol(mean_matrix)
  row_cov <- diag(row_sd, r); col_cov <- diag(col_sd, p)
  lapply(seq_len(n), function(i) {
    noise <- matrix(rnorm(r*p), r, p)
    mean_matrix + row_cov \%*\% noise \%*\% col_cov
  })
}

x_list <- c(
  simulate_matrix_group(15, mean_1),
  simulate_matrix_group(15, mean_2)
)

fit <- matrix_variate_mixture_fit(x_list, g=2, max_iter=50, verbose=TRUE)
fit$cluster
fit$pi
}

}
