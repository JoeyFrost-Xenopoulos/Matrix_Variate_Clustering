% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Matrix_Noise.R
\name{matrix_variate_noise_fit}
\alias{matrix_variate_noise_fit}
\title{Matrix-Variate Noise Mixture Clustering}
\usage{
matrix_variate_noise_fit(
  x_list,
  g,
  noise_type = c("hc", "br"),
  max_iter = 1000,
  tol = 1e-06,
  nstart = 10,
  noise_k = 1e-04,
  select_noise_k = FALSE,
  noise_k_grid = 10^seq(-8, -1, length.out = 15),
  noise_jitter = 1e-08,
  noise_pi_init = 0.05,
  use_parallel = FALSE,
  n_cores = NULL,
  verbose = FALSE
)
}
\arguments{
\item{x_list}{A non-empty list of numeric matrices, each of the same size.}

\item{g}{Integer: number of Gaussian mixture components.}

\item{noise_type}{Character: \code{"hc"} for improper constant noise or \code{"br"}
for convex-hull uniform noise.}

\item{max_iter}{Integer: maximum EM iterations.}

\item{tol}{Numeric: convergence tolerance on the log-likelihood trace.}

\item{nstart}{Integer: number of k-means restarts for initialization.}

\item{noise_k}{Numeric: constant noise height used when \code{noise_type = "hc"}.}

\item{select_noise_k}{Logical: if \code{TRUE} and \code{noise_type = "hc"}, grid
search \code{noise_k_grid} and select the fit with the smallest matrix-KS
statistic on fitted Mahalanobis distances.}

\item{noise_k_grid}{Numeric vector of candidate HC noise heights to try when
\code{select_noise_k = TRUE}. The search also augments this grid with a
matrix-dimension-aware log-scale heuristic so it can reach much smaller
densities when the data dimension grows.}

\item{noise_jitter}{Numeric: retained for compatibility; unused by the BR
convex-hull support.}

\item{noise_pi_init}{Numeric: initial mixing proportion for the noise
component.}

\item{use_parallel}{Logical: if \code{TRUE}, evaluate HC candidate noise heights
in parallel during selection.}

\item{n_cores}{Integer: number of worker processes to use when
\code{use_parallel = TRUE}.}

\item{verbose}{Logical: print iteration progress.}
}
\value{
A list containing the fitted mixture parameters, posterior
responsibilities, log-likelihood trace, and a noise summary. The returned
responsibility matrix includes the noise component in the last column, and
\code{cluster} uses \code{0} for observations assigned to noise.
}
\description{
Fits a matrix-variate Gaussian mixture model with an additional noise
component. The noise component can be either Hennig-Coretto style improper
constant noise (\code{hc}) or Banfield-Raftery style bounded uniform noise (\code{br}).
}
\details{
The Gaussian components use the same matrix-variate normal updates as the
base fitter in \code{\link[=matrix_variate_mixture_fit]{matrix_variate_mixture_fit()}}. The noise component is fixed
during the M-step and only its mixing proportion is updated.
}
\examples{
\dontrun{
set.seed(1)
x_list <- list(
  matrix(rnorm(12), 3, 4),
  matrix(rnorm(12, mean = 2), 3, 4),
  matrix(rnorm(12, mean = 8), 3, 4)
)

fit_hc <- matrix_variate_noise_fit(x_list, g = 2, noise_type = "hc")
fit_br <- matrix_variate_noise_fit(x_list, g = 2, noise_type = "br")
}

}
