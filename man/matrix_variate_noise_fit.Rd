% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Matrix_Noise.R
\name{matrix_variate_noise_fit}
\alias{matrix_variate_noise_fit}
\title{Matrix-Variate Noise Mixture Clustering with Automatic K Selection}
\usage{
matrix_variate_noise_fit(
  x_list,
  g,
  noise_type = c("hc", "br"),
  max_iter = 1000,
  tol = 1e-06,
  nstart = 100,
  noise_k = 1e-04,
  estimate_k = FALSE,
  k_grid = NULL,
  adaptive_grid = TRUE,
  noise_pi_init = 0.05,
  verbose = FALSE
)
}
\arguments{
\item{x_list}{A non-empty list of numeric matrices, each of the same size.}

\item{g}{Integer: number of Gaussian mixture components.}

\item{noise_type}{Character: \code{"hc"} for improper constant noise or \code{"br"}
for convex-hull uniform noise.}

\item{max_iter}{Integer: maximum EM iterations.}

\item{tol}{Numeric: convergence tolerance on the log-likelihood trace.}

\item{nstart}{Integer: number of k-means restarts for initialization.}

\item{noise_k}{Numeric: constant noise height used when \code{noise_type = "hc"}.
If \code{estimate_k = TRUE}, this is ignored.}

\item{estimate_k}{Logical: if TRUE, automatically select optimal noise_k
using KS goodness-of-fit test.}

\item{k_grid}{Numeric vector: grid of k values to search over when
\code{estimate_k = TRUE}. If NULL, automatically generates dimension-aware grid.}

\item{adaptive_grid}{Logical: if TRUE and k_grid is NULL, generate
dimension-aware heuristic grid based on matrix dimensions.}

\item{noise_pi_init}{Numeric: initial mixing proportion for the noise
component.}

\item{verbose}{Logical: print iteration progress.}
}
\value{
A list containing the fitted mixture parameters, posterior
responsibilities, log-likelihood trace, and a noise summary. If
\code{estimate_k = TRUE}, also includes \code{k_grid} and \code{ks_scores}.
}
\description{
Fits a matrix-variate Gaussian mixture model with a basic noise component.
The noise component can be either Hennig-Coretto style improper constant
noise (\code{hc}) or Banfield-Raftery style bounded uniform noise (\code{br}).
}
\details{
When \code{estimate_k = TRUE} and \code{noise_type = "hc"}, the optimal noise constant
is selected by:
\enumerate{
\item For each candidate k, run an EM with the noise component.
\item Filter out observations assigned to noise, and refit a proper
g-component mixture on the remaining data (starting from the
noise‑EM parameters).
\item Score the refitted model with a Kolmogorov–Smirnov test on the
Mahalanobis distances to the assigned Gaussian components.
The k that minimises the KS statistic is chosen.
}
}
