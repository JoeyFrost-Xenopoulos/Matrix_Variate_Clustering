% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Matrix.R
\name{matrix_mixture_kmeans_init}
\alias{matrix_mixture_kmeans_init}
\title{K-Means Initialization for Matrix Mixture Models}
\usage{
matrix_mixture_kmeans_init(x_list, g, nstart = 10)
}
\arguments{
\item{x_list}{A list of numeric matrices, each of dimension r × p}

\item{g}{Integer: number of mixture components}

\item{nstart}{Integer: number of k-means restarts (default: 10)}
}
\value{
A list containing:
\item{pi}{Numeric vector of length g: mixing proportions}
\item{M}{List of g matrices: component mean matrices}
\item{U}{List of g matrices: row covariance matrices}
\item{V}{List of g matrices: column covariance matrices}
\item{cluster}{Integer vector: initial cluster assignments}
}
\description{
Initializes parameters for the EM algorithm using k-means clustering on
vectorized matrices. This provides reasonable starting values for mixing
proportions, component means, and covariance matrices.
}
\details{
The initialization procedure:
\enumerate{
\item Vectorizes all matrix observations (matrix becomes a row vector)
\item Applies k-means clustering on the vectorized data
\item For each cluster, computes sample mean matrix and empirical covariances
\item Enforces identifiability constraint: tr(U) = r by scaling U and V
}

The identifiability constraint resolves the scale ambiguity between U and V:
if k > 0, then (1/k)U and kV yield the same distribution as U and V.
We enforce tr(U) = r (trace of row covariance equals row dimension).
}
\keyword{internal}
